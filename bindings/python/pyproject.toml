[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "zlicenser"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "zlicenser._lib"
